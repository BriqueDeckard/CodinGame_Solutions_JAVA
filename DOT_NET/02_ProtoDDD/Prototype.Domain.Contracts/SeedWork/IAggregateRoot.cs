namespace Prototype.Domain.Contracts.SeedWork
{
    /// <summary>
    ///     Contract for aggregate root.
    ///     Aggregates are groups of associated objects which are considered as a single whole with regard to data
    ///     modifications.
    ///     Each aggregate has a root which is an entity that will be the link between the objects inside and the objects
    ///     outside the aggregate.
    /// </summary>
    public interface IAggregateRoot
    {
    }
}